#define "allow" as string
        default "true"
        enum ["true", "false"]
        hint "是否允许删除列"

if
  @domain.sqlType == 'ALTER_TABLE_DROP_COLUMN' and
  !cast(#{allow} as bool)
then
  return false
end
