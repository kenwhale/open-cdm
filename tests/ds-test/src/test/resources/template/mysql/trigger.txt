[test]
type: createTrigger
option:
{
  "targetName": "target_name",
  "schema": "test_schema",
  "delimited": true,
  "data": {
    "name": "test_trigger",
    "triggerTable": "table",
    "triggerTime": "before",
    "triggerEvent": [
      "insert"
    ],
    "sql": "begin insert table tmp values(1,2); end"
  }
}
expect:
<<<
create trigger `test_schema`.`test_trigger` before insert 
on `test_schema`.`table`
for each row 
begin insert table tmp values(1,2); end;
>>>
