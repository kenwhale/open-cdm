[utl_inaddr_table_domain]
sql:
select
  utl_inaddr.get_host_address(t.host_value) as host_address,
  utl_inaddr.get_host_name(t.ip_value) as host_name
from catalog1.app.network_source t
expect:
{
  "contains": [
    {"class":"RdbTableDomain", "sqlType":"SELECT", "auditKind":"QUERY", "schema":"app", "table":"network_source"}
  ]
}
