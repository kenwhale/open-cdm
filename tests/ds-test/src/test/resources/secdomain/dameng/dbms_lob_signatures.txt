[dbms_lob_all_public_procedure_domains]
sql:
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.APPEND(LOB_LOC=>b1,LOB_SRC=>b2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.APPEND(LOB_LOC=>c1,LOB_SRC=>c2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.CLOSE(LOB_LOC=>b1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.CLOSE(LOB_LOC=>c1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.CLOSE(FIL_LOC=>f); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.COMPARE(LOB_1=>b1,LOB_2=>b2,AMOUNT=>4,OFFSET_1=>1,OFFSET_2=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.COMPARE(LOB_1=>c1,LOB_2=>c2,AMOUNT=>4,OFFSET_1=>1,OFFSET_2=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.COMPARE(LOB_1=>f,LOB_2=>f,AMOUNT=>4,OFFSET_1=>1,OFFSET_2=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.CONVERTTOBLOB(DEST_LOB=>b1,SRC_CLOB=>c1,AMOUNT=>4,DEST_OFFSET=>off1,SRC_OFFSET=>off2,BLOB_CSID=>0,LANG_CONTEXT=>lang,WARNING=>warning); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.CONVERTTOCLOB(DEST_LOB=>c1,SRC_BLOB=>b1,AMOUNT=>4,DEST_OFFSET=>off1,SRC_OFFSET=>off2,BLOB_CSID=>0,LANG_CONTEXT=>lang,WARNING=>warning); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.COPY(DEST_LOB=>b1,SRC_LOB=>b2,AMOUNT=>4,DEST_OFFSET=>1,SRC_OFFSET=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.COPY(DEST_LOB=>c1,SRC_LOB=>c2,AMOUNT=>4,DEST_OFFSET=>1,SRC_OFFSET=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.CREATETEMPORARY(LOB_LOC=>b1,CACHE=>TRUE,DUR=>DBMS_LOB.SESSION); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.CREATETEMPORARY(LOB_LOC=>c1,CACHE=>TRUE,DUR=>DBMS_LOB.SESSION); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.ERASE(LOB_LOC=>b1,AMOUNT=>amt,OFFSET=>2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.ERASE(LOB_LOC=>c1,AMOUNT=>amt,OFFSET=>2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FILECLOSE(FIL_LOC=>f); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FILECLOSEALL(); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.FILEEXISTS(FILE_LOC=>f); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FILEGETNAME(FILE_LOC=>f,DIR_ALIAS=>dir_name,FILENAME=>file_name); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.FILEISOPEN(FILE_LOC=>f); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FILEOPEN(FILE_LOC=>f,OPEN_MODE=>DBMS_LOB.FILE_READONLY); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FRAGMENT_DELETE(LOB_LOC=>b1,AMOUNT=>2,OFFSET=>2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FRAGMENT_DELETE(LOB_LOC=>c1,AMOUNT=>2,OFFSET=>2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FRAGMENT_INSERT(LOB_LOC=>b1,AMOUNT=>2,OFFSET=>2,BUFFER=>CAST('5859' AS VARBINARY(8))); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FRAGMENT_INSERT(LOB_LOC=>c1,AMOUNT=>2,OFFSET=>2,BUFFER=>'XY'); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FRAGMENT_MOVE(LOB_LOC=>b1,AMOUNT=>1,SRC_OFFSET=>1,DEST_OFFSET=>3); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FRAGMENT_MOVE(LOB_LOC=>c1,AMOUNT=>1,SRC_OFFSET=>1,DEST_OFFSET=>3); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FRAGMENT_REPLACE(LOB_LOC=>b1,OLD_AMOUNT=>2,NEW_AMOUNT=>2,OFFSET=>2,BUFFER=>CAST('5859' AS VARBINARY(8))); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FRAGMENT_REPLACE(LOB_LOC=>c1,OLD_AMOUNT=>2,NEW_AMOUNT=>2,OFFSET=>2,BUFFER=>'XY'); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FREETEMPORARY(LOB_LOC=>b1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.FREETEMPORARY(LOB_LOC=>c1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin n:=DBMS_LOB.GET_STORAGE_LIMIT(LOB_LOC=>b1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin n:=DBMS_LOB.GET_STORAGE_LIMIT(LOB_LOC=>c1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.GETCHUNKSIZE(LOB_LOC=>b1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.GETCHUNKSIZE(LOB_LOC=>c1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin n:=DBMS_LOB.GETLENGTH(LOB_1=>b1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin n:=DBMS_LOB.GETLENGTH(LOB_1=>c1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin n:=DBMS_LOB.GETLENGTH(FILE_LOC=>f); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin n:=DBMS_LOB.INSTR(LOB_LOC=>b1,PATTERN=>CAST('4142' AS VARBINARY(8)),OFFSET=>1,NTH=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin n:=DBMS_LOB.INSTR(LOB_LOC=>c1,PATTERN=>'AB',OFFSET=>1,NTH=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.ISOPEN(LOB_LOC=>b1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.ISOPEN(LOB_LOC=>c1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.ISOPEN(FILE_LOC=>f); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.ISTEMPORARY(LOB_LOC=>b1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin i:=DBMS_LOB.ISTEMPORARY(LOB_LOC=>c1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.LOADBLOBFROMFILE(DEST_LOB=>b1,SRC_BFILE=>f,AMOUNT=>4,DEST_OFFSET=>off1,SRC_OFFSET=>off2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.LOADCLOBFROMFILE(DEST_LOB=>c1,SRC_BFILE=>f,AMOUNT=>4,DEST_OFFSET=>off1,SRC_OFFSET=>off2,BFILE_CSID=>0,LANG_CONTEXT=>lang,WARNING=>warning); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.LOADFROMFILE(DEST_LOB=>b1,SRC_FILE=>f,AMOUNT=>4,DEST_OFFSET=>off1,SRC_OFFSET=>off2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.OPEN(LOB_LOC=>b1,OPEN_MODE=>DBMS_LOB.LOB_READWRITE); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.OPEN(LOB_LOC=>c1,OPEN_MODE=>DBMS_LOB.LOB_READWRITE); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.OPEN(FILE_LOC=>f,OPEN_MODE=>DBMS_LOB.FILE_READONLY); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.READ(LOB_LOC=>b1,AMOUNT=>read_amount,OFFSET=>1,BUFFER=>vb); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.READ(LOB_LOC=>c1,AMOUNT=>read_amount,OFFSET=>1,BUFFER=>vc); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.READ(FILE_LOC=>f,AMOUNT=>read_amount,OFFSET=>1,BUFFER=>vb); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin vb:=DBMS_LOB.SUBSTR(LOB_LOC=>b1,AMOUNT=>4,OFFSET=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin vc:=DBMS_LOB.SUBSTR(LOB_LOC=>c1,AMOUNT=>4,OFFSET=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin vb:=DBMS_LOB.SUBSTR(FILE_LOC=>f,AMOUNT=>4,OFFSET=>1); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.TRIM(LOB_LOC=>b1,NEWLEN=>2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.TRIM(LOB_LOC=>c1,NEWLEN=>2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.TRIM_LOB(LOB_LOC=>b1,NEWLEN=>2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.TRIM_LOB(LOB_LOC=>c1,NEWLEN=>2); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.WRITE(LOB_LOC=>b1,AMOUNT=>2,OFFSET=>2,BUFFER=>CAST('5859' AS VARBINARY(8))); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.WRITE(LOB_LOC=>c1,AMOUNT=>2,OFFSET=>2,BUFFER=>'XY'); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.WRITEAPPEND(LOB_LOC=>b1,AMOUNT=>2,BUFFER=>CAST('5859' AS VARBINARY(8))); end;
/
declare
  b1 blob; b2 blob; c1 clob; c2 clob;
  f bfile:=BFILENAME('CODEX_LOB_SIG_DIR','dm.ini');
  amt bigint:=2; off1 bigint:=1; off2 bigint:=1; n bigint;
  lang integer:=0; warning integer; i integer; read_amount integer:=4;
  vb varbinary(32767); vc varchar2(32767);
  dir_name varchar2(128); file_name varchar2(256); begin DBMS_LOB.WRITEAPPEND(LOB_LOC=>c1,AMOUNT=>2,BUFFER=>'XY'); end;
expect:
{"contains":[
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"},
  {"class":"RdbCallDomain","sqlType":"CALL_PROG_OBJ","auditKind":"CALL","schema":"DBMS_LOB"}
]}
