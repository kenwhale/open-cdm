<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clougence.clouddm.platform.dal.mapper.execution.DmExecSqlAuditMapper">
    <resultMap id="dmSqlAudit" type="com.clougence.clouddm.platform.dal.model.execution.DmExecSqlAuditDO">
        <result column="id" property="id"/>
        <result column="gmt_create" property="gmtCreate"/>
        <result column="gmt_modified" property="gmtModified"/>
        <result column="operate_time" property="operateTime"/>
        <result column="end_time" property="endTime"/>
        <result column="uid" property="uid"/>
        <result column="user_name" property="userName"/>
        <result column="log_ip" property="logIp"/>
        <result column="client_ip" property="clientIp"/>
        <result column="work_seq_number" property="workSeqNumber"/>
        <result column="exec_sql" property="execSql"/>
        <result column="original_sql" property="originalSql"/>
        <result column="requester" property="requester"/>
        <result column="query_id" property="queryId"/>
        <result column="behaviors" property="behaviors"
                typeHandler="com.clougence.clouddm.platform.dal.handler.json.BehaviorRelationListTypeHandler"/>
        <result column="session_id" property="sessionId"/>
        <result column="ds_id" property="dsId"/>
        <result column="affect_line" property="affectLine"/>
        <result column="status" property="status"/>
        <result column="ds_desc" property="dsDesc"/>
        <result column="message" property="message"/>
        <result column="data_source_type" property="dataSourceType"/>
    </resultMap>

    <sql id="dmSqlAuditDOMap">
        id,
        gmt_create,
        gmt_modified,
        operate_time,
        end_time,
        uid,
        user_name,
        affect_line,
        ds_id,
        ds_desc,
        session_id,
        status,
        log_ip,
        client_ip,
        work_seq_number,
        requester,
        query_id,
        behaviors,
        exec_sql,
        original_sql,
        message,
        data_source_type
    </sql>

    <sql id="sqlAuditCondition">
        AND uid IN (
            SELECT scoped_user.uid
            FROM dm_auth_user scoped_user
            LEFT JOIN dm_auth_user primary_user ON primary_user.id = scoped_user.parent_id
            WHERE scoped_user.uid = #{puid} OR primary_user.uid = #{puid}
        )
        <if test="uid != null and uid != ''">
            AND uid = #{uid}
        </if>
        <if test="dateStart != null">
            <![CDATA[ AND COALESCE(operate_time, gmt_create) >= #{dateStart} ]]>
        </if>
        <if test="dateEnd != null">
            <![CDATA[ AND COALESCE(operate_time, gmt_create) <= #{dateEnd} ]]>
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="requester != null">
            AND requester = #{requester}
        </if>
        <if test="dsId != null">
            AND ds_id = #{dsId}
        </if>
    </sql>

    <update id="markRunningByQueryId">
        UPDATE dm_exec_sql_audit
        SET status = 'RUNNING',
            session_id = #{sessionId},
            client_ip = #{clientIp},
            work_seq_number = #{workSeqNumber},
            operate_time = #{operateTime},
            affect_line = 0,
            end_time = NULL,
            message = NULL,
            gmt_modified = CURRENT_TIMESTAMP
        WHERE query_id = #{queryId}
          AND status IN ('PENDING', 'FAILURE', 'ERROR')
    </update>

    <update id="completeByQueryId">
        UPDATE dm_exec_sql_audit
        SET status = #{status},
            session_id = COALESCE(#{sessionId}, session_id),
            affect_line = #{affectLine},
            message = #{message},
            end_time = #{time},
            gmt_modified = CURRENT_TIMESTAMP
        WHERE query_id = #{queryId}
          AND status IN ('PENDING', 'RUNNING')
    </update>

    <update id="confirmSession">
        UPDATE dm_exec_sql_audit
        SET status = 'SUCCESS'
        WHERE session_id = #{sessionId}
          AND status = 'WAIT_CONFIRM'
    </update>

    <update id="rollbackSession">
        UPDATE dm_exec_sql_audit
        SET status = 'ROLLBACK'
        WHERE session_id = #{sessionId}
          AND status = 'WAIT_CONFIRM'
    </update>

    <update id="updateErrorSql">
        UPDATE dm_exec_sql_audit
        SET status = 'ERROR'
        WHERE work_seq_number = #{wsn}
          AND status IN ('RUNNING', 'WAIT_CONFIRM')
    </update>

    <delete id="deleteAuditBeforeDate">
        DELETE FROM dm_exec_sql_audit
        WHERE uid IN (
            SELECT scoped_user.uid
            FROM dm_auth_user scoped_user
            LEFT JOIN dm_auth_user primary_user ON primary_user.id = scoped_user.parent_id
            WHERE scoped_user.uid = #{puid} OR primary_user.uid = #{puid}
        )
          AND #{date} > end_time
        LIMIT 500
    </delete>

    <select id="queryByQueryId" resultMap="dmSqlAudit">
        SELECT
        <include refid="dmSqlAuditDOMap"/>
        FROM dm_exec_sql_audit
        WHERE query_id = #{queryId}
    </select>

    <select id="queryWaitConfirmBySessionId" resultMap="dmSqlAudit">
        SELECT
        <include refid="dmSqlAuditDOMap"/>
        FROM dm_exec_sql_audit
        WHERE session_id = #{sessionId}
          AND status = 'WAIT_CONFIRM'
        ORDER BY id
    </select>

    <select id="queryByCondition" resultMap="dmSqlAudit">
        SELECT
        <include refid="dmSqlAuditDOMap"/>
        FROM dm_exec_sql_audit
        WHERE 1 = 1
        <if test="startId != 0">
            <![CDATA[ AND id < #{startId} ]]>
        </if>
        <if test="uid != null and uid != ''">
            AND uid = #{uid}
        </if>
        <if test="dateStart != null">
            <![CDATA[ AND gmt_create >= #{dateStart} ]]>
        </if>
        <if test="dateEnd != null">
            <![CDATA[ AND gmt_create <= #{dateEnd} ]]>
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="requester != null">
            AND requester = #{requester}
        </if>
        <if test="dsId != null">
            AND ds_id = #{dsId}
        </if>
        ORDER BY id DESC
        LIMIT #{pageSize}
    </select>

    <select id="pageByCondition" resultMap="dmSqlAudit">
        SELECT
        <include refid="dmSqlAuditDOMap"/>
        FROM dm_exec_sql_audit
        WHERE 1 = 1
        <include refid="sqlAuditCondition"/>
        ORDER BY COALESCE(operate_time, gmt_create) DESC, id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*)
        FROM dm_exec_sql_audit
        WHERE 1 = 1
        <include refid="sqlAuditCondition"/>
    </select>
</mapper>
